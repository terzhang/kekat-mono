{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start:server": "lerna run start --scope=@kekat/server",
    "dev": "npm run start:server && lerna run dev --scope=@kekat/client",
    "start": "npm run start:server && lerna run start --scope=@kekat/client"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/typeorm/**",
      "**/typeorm",
      "**/connect-redis/**",
      "**/connect-redis"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^3.7.1",
    "@typescript-eslint/parser": "^3.7.1",
    "eslint": "^7.5.0",
    "typescript": "^3.9.7"
  }
}
