{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "start:server": "lerna run start --scope=typegraphql",
    "dev": "npm run start:server && lerna run dev --scope=kekat",
    "start": "npm run start:server && lerna run start --scope=kekat"
  },
  "workspaces": [
    "packages/*"
  ]
}
